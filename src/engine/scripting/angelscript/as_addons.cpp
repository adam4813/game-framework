// Compile AngelScript add-ons required by this project.
// The angelscript vcpkg "addons" feature installs these as source files,
// not as a pre-compiled library, so we include them here directly.

#include <angelscript/scriptstdstring/scriptstdstring.cpp>
